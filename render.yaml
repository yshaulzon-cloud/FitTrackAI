services:
  - type: web
    name: bodysync-api
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install --prefix server
    startCommand: node server/server.js
    healthCheckPath: /health
    envVars:
      # Sensitive vars — set the actual values in Render dashboard
      # (Settings → Environment) so they are not committed to Git.
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      # Brevo HTTP email API — Render free tier blocks outbound SMTP, so
      # password-reset mail goes through Brevo over https instead.
      - key: BREVO_API_KEY
        sync: false
      # Brevo-verified sender address (defaults to EMAIL_USER if unset).
      - key: EMAIL_FROM
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      # Public configuration — safe to commit.
      - key: CORS_ORIGIN
        value: https://app.digtal-c.co.il
      - key: NODE_VERSION
        value: 20.11.0
