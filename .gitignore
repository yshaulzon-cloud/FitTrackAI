# Dependencies & build artifacts
node_modules/
dist/
*.log

# Local env / secrets — server/.env contains real secrets and must never
# be committed. .env.production / .env.example are safe (no secrets).
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Project-local data not relevant to the app
photos/
מחקרים/
תמונות אנימציה/
לוגו.png

# Test screenshots & one-off assets
screenshot-extras.mjs.bin
*.pdf

# Build cache
.cache/

# Android signing — NEVER commit keystores or their passwords
keystores/
*.keystore
*.jks
key.properties

# Capacitor build artifacts (regenerated on every build)
android/app/build/
android/build/
android/.gradle/
android/local.properties

# Claude Code worktrees and local agent state
.claude/worktrees/
.claude/settings.local.json

# Local tooling scripts & generated assets (not part of the app)
cookies.txt
_test_search.txt
download.mjs
make-*.mjs
screenshot-*.mjs
separate*.py
photos2/
photos3/
photos4/
play-assets/
store-screenshots/
store-screenshots-en/
מסכי פתיחה/
מסכים הגדרות/
מערכת אימונים/
*.docx
